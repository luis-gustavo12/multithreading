[package]
name = "rust"
version = "0.1.0"
edition = "2024"
description = "A file processor parser that is supposed to run asynchoronously"
[dependencies]
clap = { version = "4.5", features = ["derive"]}
chrono = "0.4"